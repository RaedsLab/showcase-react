const styles = theme => ({
  wrapper: {},
  select: {
    flexGrow: 1
  },
  root: {
    display: 'flex',
    flexWrap: 'wrap'
  },
  formControl: {
    margin: theme.spacing.unit,
    width: '90%'
  }
});

export default styles;
