const styles = theme => ({
  description: {
    padding: theme.spacing.unit * 2,
    textAlign: 'justify',
    color: theme.palette.text.primary,
    height: 120,
    marginTop: 20
  }
});

export default styles;
