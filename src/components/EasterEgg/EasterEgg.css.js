const styles = theme => ({
  paper: {
    padding: theme.spacing.unit * 2,
    display: 'flex',
    flexDirection: 'column',
    flex: 1,
    flexShrink: 1,
    flexGrow: 1
  }
});

export default styles;
